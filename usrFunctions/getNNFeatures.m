function nnFeatures = getNNFeatures(ptCloud, radius)
% 
% steps = (1:ptCloud.Count/1000:ptCloud.Count)';
% 
% y = ptCloud.Location(:,2);
% y = sort(y);
% 
% 
% [indices,dists] = findNeighborsInRadius(ptCloud,point,radius);
end